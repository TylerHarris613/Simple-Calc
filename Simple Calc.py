# Prompt the User: Ask the user to input two numbers and choose an operation (addition, subtraction, multiplication, or division).
# Handle Operations: Based on the user's input, perform the corresponding operation.
# Error Handling: Ensure that division by zero is handled gracefully (give an error message instead of crashing).
# Display the Result: After the calculation, display the result to the user.
